#include <bits/stdc++.h>
using namespace std;
#define pb push_back
#define sz(x) ((int)(x).size())
#define all(x) (x).begin(), (x).end()

int main()
{
  unsigned int c = thread::hardware_concurrency();
  cout << c << endl;
  return 0;
}
